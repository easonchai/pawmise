module pawmise::market {
    /// This is just a "marker" type for the lending market.
    /// It doesn't need any functionality - it's just used as a type parameter.
   public struct PAWMISE_MARKET has drop {}
}